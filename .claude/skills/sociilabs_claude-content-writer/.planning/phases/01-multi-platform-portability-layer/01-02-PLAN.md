---
phase: 01-multi-platform-portability-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-writer-commands.js
  - package.json
autonomous: true
requirements: [VERIFY-02]

must_haves:
  truths:
    - "Running `node scripts/verify-writer-commands.js` performs a structural lint (no browser, no live Claude Code session) and exits non-zero the moment any of its three checks fails"
    - "The script has no new npm dependency — it parses restricted flat frontmatter with plain string/regex operations, consistent with the repo's zero-dependency footprint"
  artifacts:
    - path: "scripts/verify-writer-commands.js"
      provides: "VERIFY-02's automated check: --check=schema, --check=neutralize, --check=migration, and a default full run"
  key_links:
    - from: "scripts/verify-writer-commands.js"
      to: "package.json"
      via: "an npm script entry so the check has one canonical invocation"
      pattern: "\"verify\":\\s*\"node scripts/verify-writer-commands.js\""
---

<objective>
Build the one automated test surface this phase (and this repo) has: a dependency-free Node
script that structurally lints the migration this phase performs, without requiring a browser or
a live Claude Code session.

Purpose: VERIFY-02 is explicitly "the one adapter that CAN be verified without a human clicking
through a GUI" (per ROADMAP/RESEARCH). Every later plan in this phase that migrates a file or adds
a reference doc runs this script as part of its own `<verify>` step, so it must exist before any
migration work lands.
Output: `scripts/verify-writer-commands.js`, wired into `package.json` as `npm run verify`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
@.planning/phases/01-multi-platform-portability-layer/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the structural lint script</name>
  <files>scripts/verify-writer-commands.js</files>
  <read_first>
    .planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md ("Don't Hand-Roll" section
    — restricted regex-parseable frontmatter subset argument, "Validation Architecture" >
    "Phase Requirements -> Test Map"), scripts/postinstall.js (existing Node script style/idioms
    in this repo, for consistency)
  </read_first>
  <behavior>
    - `node scripts/verify-writer-commands.js --check=schema`: reads skills/state-schema.md and
      confirms every one of the 28 PROJECT-STATE.md frontmatter key names it must define
      (phase, platform, format, topic, angle, audience, awareness_stage, goal, framework, length,
      cta, research_urls, key_points, seo_primary_keyword, seo_secondary_keywords,
      seo_meta_title, seo_meta_description, seo_slug, platform_conventions_file, voice_notes,
      proof_points, cta_placement, draft_word_count, cta_expanded, seo_score,
      ai_patterns_fixed, manual_check, updated_at) is present as a listed key; prints one line
      per missing key and exits 1 if any are missing, else prints a pass summary and exits 0.
    - `node scripts/verify-writer-commands.js --check=neutralize`: greps every file under
      references/ for the case-sensitive substring that is the two words "Content Writer"
      immediately preceded by an open bracket; any match is reported with file:line and causes
      exit 1; zero matches exits 0.
    - `node scripts/verify-writer-commands.js --check=migration`: greps skills/shared-context.md
      and every file under skills/writer/ for the phrase "check memory first" (case-insensitive)
      — presence means a file still describes memory as consulted before the state file, which
      is the pre-migration pattern; any match is reported with file:line and causes exit 1; also
      confirms skills/shared-context.md contains the substring "PROJECT-STATE.md" (the new
      file-based schema is actually referenced); missing that substring also causes exit 1.
    - `node scripts/verify-writer-commands.js` with no flags: runs all three checks in sequence,
      prints a per-check pass/fail summary, and exits 0 only if all three passed.
    - `node scripts/verify-writer-commands.js --self-test`: runs an internal self-test that
      writes two throwaway fixture files into a temp directory (one with all 28 required schema
      keys present, one missing three of them), runs the schema-check logic against each, asserts
      the first passes and the second fails with exactly the 3 expected missing keys named, then
      deletes the temp directory; exits 0 if both assertions hold, exits 1 and prints which
      assertion failed otherwise. This proves the check logic is correct independent of the rest
      of the repo's current migration state.
  </behavior>
  <action>
    Implement scripts/verify-writer-commands.js as a single dependency-free Node script (CommonJS,
    matching scripts/postinstall.js's style) using only `fs`, `path`, and `process.argv` — no new
    npm dependency. Structure it as three exported check functions (checkSchema, checkNeutralize,
    checkMigration) plus a thin CLI wrapper at the bottom that dispatches on `--check=<name>`,
    runs all three when no flag is given, and runs the self-test fixtures described above when
    `--self-test` is passed. Each check function returns `{ pass: boolean, details: string[] }`;
    the CLI wrapper prints `details` and sets `process.exitCode = 1` on any failure. Frontmatter
    parsing anywhere in this script must use a restricted flat-key regex/string-split approach
    (one `key: value` per line, no nested block parsing) — never add js-yaml, gray-matter, or any
    other new dependency; do not modify the `dependencies` block in package.json for this task.
  </action>
  <verify>
    <automated>node scripts/verify-writer-commands.js --self-test; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-writer-commands.js --self-test` prints an explicit pass line for both
      the "all keys present" fixture and the "missing keys" fixture, and exits 0
    - `node scripts/verify-writer-commands.js --check=schema` runs against the real repo without
      throwing (skills/state-schema.md exists from the sibling plan in this same wave; if the two
      plans in this wave execute out of order, this check must fail cleanly with a clear "file
      not found" message, not crash with an unhandled exception)
    - Running the script with an unrecognized `--check=` value prints a usage message and exits 1
      rather than silently doing nothing
    - No entry was added to package.json's `dependencies` block
  </acceptance_criteria>
  <done>
    scripts/verify-writer-commands.js exists, implements all three checks plus `--self-test`, and
    the self-test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the script into package.json</name>
  <files>package.json</files>
  <read_first>package.json (current scripts block)</read_first>
  <action>
    Add a `"verify": "node scripts/verify-writer-commands.js"` entry to package.json's `scripts`
    block, alongside the existing `postinstall` entry. Do not modify the `dependencies`,
    `files`, or `engines` blocks in this task.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); process.exit(p.scripts && p.scripts.verify === 'node scripts/verify-writer-commands.js' ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify` invokes scripts/verify-writer-commands.js (confirmed via the automated
      check above reading package.json directly, not by trusting npm's own output)
    - package.json's `dependencies` array is byte-for-byte unchanged from before this task
  </acceptance_criteria>
  <done>package.json has a `verify` script entry pointing at the new script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Filesystem paths passed via CLI flags -> `fs` calls | The script only ever reads fixed, hardcoded repo-relative paths (skills/, references/) — it never accepts a path from `--check=` or any other flag as a literal filesystem path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-03 | Tampering | scripts/verify-writer-commands.js self-test temp files | low | mitigate | Self-test writes its fixtures under `os.tmpdir()` with a unique per-run subdirectory name and deletes it in a `finally` block, never writes inside the repo tree |
| T-01-04 | Denial of Service | scripts/verify-writer-commands.js file reads | low | accept | Script only reads a small, fixed set of markdown files already in the repo; no user-controlled input, no risk of unbounded read |

</threat_model>

<verification>
`node scripts/verify-writer-commands.js --self-test` exits 0. `npm run verify` is a valid npm
script that invokes the new file.
</verification>

<success_criteria>
scripts/verify-writer-commands.js exists, implements schema/neutralize/migration checks plus a
self-test, adds zero new dependencies, and is wired into package.json as `npm run verify`.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-02-SUMMARY.md` when done
</output>
