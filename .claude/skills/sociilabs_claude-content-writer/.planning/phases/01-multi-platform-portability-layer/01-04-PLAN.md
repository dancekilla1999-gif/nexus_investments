---
phase: 01-multi-platform-portability-layer
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - skills/writer/discuss.md
  - skills/writer/plan.md
  - skills/writer/execute.md
  - skills/writer/verify.md
  - skills/writer/ship.md
autonomous: true
requirements: [ADAPTER-01]

must_haves:
  truths:
    - "Each of the five workflow-phase commands (discuss, plan, execute, verify, ship) reads its predecessor's state from content-writer-output/profile/PROJECT-STATE.md and writes its own output back to that same file, setting the phase field to match, with memory sync as a secondary step"
    - "Running the five commands in sequence today (before this plan) and after this plan produces the same user-visible prompts, questions, and outputs — only the storage backend changed"
  artifacts:
    - path: "skills/writer/discuss.md"
      provides: "Discussion phase writes phase: discuss + Discussion Brief section to PROJECT-STATE.md"
    - path: "skills/writer/ship.md"
      provides: "Ship phase sets phase: complete instead of deleting memory keys as the primary action"
  key_links:
    - from: "skills/writer/plan.md"
      to: "skills/writer/discuss.md"
      via: "plan.md's Step 1 load reads the same PROJECT-STATE.md file discuss.md's Step 4 just wrote"
      pattern: "PROJECT-STATE.md"
---

<objective>
Migrate the five sequential workflow-phase commands (discuss -> plan -> execute -> verify -> ship)
so each one's "load state" and "save state" steps target the portable PROJECT-STATE.md file
defined in skills/state-schema.md, with Claude Code's memory tool becoming a secondary cache
write, not the primary save target.

Purpose: ADAPTER-01 requires the migration to happen now, as source of truth, without changing
what the user experiences running these commands. This is the core of the five-phase workflow —
regression risk here is the highest in the phase.
Output: skills/writer/discuss.md, plan.md, execute.md, verify.md, ship.md all migrated to file-first
state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate discuss, plan, and execute</name>
  <files>skills/writer/discuss.md, skills/writer/plan.md, skills/writer/execute.md</files>
  <read_first>
    skills/writer/discuss.md (full), skills/writer/plan.md (full), skills/writer/execute.md
    (full), skills/shared-context.md (post-migration version from plan 01-03, "Project &amp;
    Profile State Fields" and "Storage rules" sections), skills/state-schema.md (Project State
    Schema section — exact frontmatter key names and the four body headings)
  </read_first>
  <action>
    In skills/writer/discuss.md: change "Step 1: Profile check" to read PROFILE.md /
    PROJECT-STATE.md first (memory as secondary cache, per the migrated shared-context.md rule).
    Change "Step 4: Save state" so instead of only saving to a memory key, it writes/updates
    content-writer-output/profile/PROJECT-STATE.md: set frontmatter `phase: discuss`, set
    platform, format, topic, angle, audience, awareness_stage, goal, framework, length, cta,
    research_urls, key_points to the gathered values (using exactly those field names from
    skills/state-schema.md), and write the one-paragraph brief text under the
    "## Discussion Brief" body heading; then mirror the same values into Claude Code's memory
    tool as a secondary cache step. In skills/writer/plan.md: change "Step 1: Load state" to read
    PROJECT-STATE.md (checking `phase: discuss` is present; if the file is missing, keep the
    existing "No discussion state found" message). Change "Step 6: Save plan state" to update
    PROJECT-STATE.md: set `phase: plan`, set seo_primary_keyword, seo_secondary_keywords,
    seo_meta_title, seo_meta_description, seo_slug, platform_conventions_file, voice_notes,
    proof_points, cta_placement fields, and write the full outline under the "## Outline" body
    heading; mirror to memory as a secondary cache step. In skills/writer/execute.md: change
    "Step 1: Load all state" to read PROJECT-STATE.md's Discussion Brief and Outline sections plus
    its frontmatter fields (checking `phase: plan` is present; keep the existing "No plan found"
    message if missing). Change "Step 7: Save draft state" to update PROJECT-STATE.md: set
    `phase: execute`, set draft_word_count and cta_expanded fields, and write the full draft under
    the "## Draft" body heading; mirror to memory as a secondary cache step. In all three files,
    do not change any user-facing prompt text, question wording, or the confirm-before-advancing
    instructions — only the load/save mechanics change.
  </action>
  <verify>
    <automated>grep -c "PROJECT-STATE.md" skills/writer/discuss.md skills/writer/plan.md skills/writer/execute.md | awk -F: '{if ($2==0) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Each of discuss.md, plan.md, execute.md contains at least one reference to
      content-writer-output/profile/PROJECT-STATE.md in its load and save steps
    - discuss.md's save step sets `phase: discuss`, plan.md's save step sets `phase: plan`,
      execute.md's save step sets `phase: execute` — each using that exact field name
    - None of the three files' user-facing question text, framework-selection guidance, or
      confirmation prompts changed from the pre-migration version (diff shows only the Load
      state / Save state step bodies changed)
    - Memory-tool writes are described as occurring after the file write in all three files, never
      before or instead of it
  </acceptance_criteria>
  <done>
    discuss.md, plan.md, and execute.md read and write PROJECT-STATE.md as their primary state
    target, set the correct phase field value at the end of each, and mirror to memory only as a
    secondary step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate verify and ship</name>
  <files>skills/writer/verify.md, skills/writer/ship.md</files>
  <read_first>
    skills/writer/verify.md (full), skills/writer/ship.md (full), skills/state-schema.md (Project
    State Schema section)
  </read_first>
  <action>
    In skills/writer/verify.md: change "Step 1: Load draft" to read PROJECT-STATE.md's "## Draft"
    body section and its frontmatter (checking `phase: execute` is present; keep the existing
    "No draft found" message if missing). Change "Step 6: Save verified state" to update
    PROJECT-STATE.md: set `phase: verify`, set seo_score, ai_patterns_fixed, manual_check fields,
    and write the final corrected content under the "## Verified Content" body heading; mirror to
    memory as a secondary cache step. In skills/writer/ship.md: change "Step 1: Load verified
    content" to read PROJECT-STATE.md's "## Verified Content" body section plus the Discussion
    Brief and Outline sections for metadata (checking `phase: verify` is present; keep the
    existing "No verified content found" message if missing). Replace "Step 7: Clear project
    state from memory" so its primary action is setting PROJECT-STATE.md's frontmatter `phase` to
    `complete` (do not delete the file — a completed PROJECT-STATE.md is what tells the phase
    state machine to route back to /writer:discuss for a new project); keep clearing the
    equivalent Claude Code memory-tool entries as a secondary step, and keep the existing
    instruction not to touch the writer-profile memory entries. Do not change any of ship.md's
    filename-generation, output-path, frontmatter-writing, or publishing-notes logic — only the
    load step and the former "Step 7" state-clearing step change.
  </action>
  <verify>
    <automated>grep -c "phase: complete" skills/writer/ship.md && grep -c "PROJECT-STATE.md" skills/writer/verify.md skills/writer/ship.md | awk -F: '{if ($2==0) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - verify.md's save step sets `phase: verify` and writes to the "## Verified Content" heading
    - ship.md's final state-clearing step sets `phase: complete` in PROJECT-STATE.md rather than
      deleting the file outright
    - ship.md's filename generation, output directory logic, frontmatter template, and
      publishing-notes sections are byte-for-byte unchanged from the pre-migration version
    - `node scripts/verify-writer-commands.js --check=migration` no longer flags verify.md or
      ship.md as containing the pre-migration "check memory first" phrase
  </acceptance_criteria>
  <done>
    verify.md and ship.md are file-first; ship.md marks the project complete in PROJECT-STATE.md
    instead of deleting memory-only state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PROJECT-STATE.md Draft/Verified Content sections -> ship.md's file-write step | Content flows from an LLM-generated draft, through a file the user could hand-edit, into a file written to content-writer-output/ with the user's name in frontmatter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-03 | Tampering | ship.md reading a hand-edited PROJECT-STATE.md | low | accept | The user editing their own local state file before shipping their own content is expected, normal usage, not an attack surface |
| T-02-04 | Repudiation | phase field silently advancing without explicit confirmation text change | low | mitigate | Each file's existing "present and confirm before advancing" instruction is left unchanged, so the user still explicitly confirms before the phase field is set |

</threat_model>

<verification>
`node scripts/verify-writer-commands.js --check=migration` passes for all five files touched
across this plan and 01-03. Diff review confirms no user-facing prompt text changed.
</verification>

<success_criteria>
All five workflow-phase commands read and write PROJECT-STATE.md as primary state, in the exact
field names from skills/state-schema.md, with identical user-facing behavior to before migration.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-04-SUMMARY.md` when done
</output>
