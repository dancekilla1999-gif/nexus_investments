---
phase: 01-multi-platform-portability-layer
plan: 05
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - skills/writer/profile-create.md
  - skills/writer/profile-view.md
  - skills/writer/profile-edit.md
  - skills/writer/profile-delete.md
  - skills/writer/next.md
  - skills/writer/status.md
  - skills/writer/help.md
  - skills/writer/update.md
autonomous: true
requirements: [ADAPTER-01]

must_haves:
  truths:
    - "The four profile commands (create/view/edit/delete) treat PROFILE.md/PRODUCTS.md/CTAS.md/CASE-STUDIES.md as authoritative, writing memory only as a secondary cache"
    - "/writer:next and /writer:status detect the current phase by reading PROJECT-STATE.md's phase field, not by checking for the presence of specific memory keys"
  artifacts:
    - path: "skills/writer/profile-create.md"
      provides: "Profile creation writes files first, memory second (ADAPTER-01)"
    - path: "skills/writer/next.md"
      provides: "Phase auto-advance keyed on PROJECT-STATE.md's phase field"
  key_links:
    - from: "skills/writer/next.md"
      to: "skills/state-schema.md"
      via: "next.md's phase-detection table matches the Phase State Machine table in state-schema.md exactly"
      pattern: "phase: discuss|phase: plan|phase: execute|phase: verify"
---

<objective>
Migrate the four profile commands and the four utility commands to the file-first storage model,
completing the ADAPTER-01 migration alongside plans 01-03 and 01-04.

Purpose: ADAPTER-01 requires every `/writer:*` command's internal storage to move to the portable
schema, not just the five sequential workflow commands. `/writer:next` and `/writer:status` in
particular are the two commands that implement phase detection for the user, and must key off the
new `phase` field, matching skills/state-schema.md's Phase State Machine exactly.
Output: skills/writer/profile-create.md, profile-view.md, profile-edit.md, profile-delete.md,
next.md, status.md, help.md, update.md all migrated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the four profile commands</name>
  <files>
    skills/writer/profile-create.md, skills/writer/profile-view.md,
    skills/writer/profile-edit.md, skills/writer/profile-delete.md
  </files>
  <read_first>
    skills/writer/profile-create.md (full), skills/writer/profile-view.md (full),
    skills/writer/profile-edit.md (full), skills/writer/profile-delete.md (full),
    references/profile-management.md (post-neutralization version from plan 01-01 — its
    "Profile file structure" section), skills/state-schema.md (Profile Schema section)
  </read_first>
  <action>
    In skills/writer/profile-create.md: change "Step 3: Confirm and save" so its primary save
    action is writing content-writer-output/profile/PROFILE.md, PRODUCTS.md, CTAS.md,
    CASE-STUDIES.md using the exact headings from skills/state-schema.md's Profile Schema
    section, with the equivalent Claude Code memory-tool entries written as a secondary cache
    step immediately after (reverse the current file/memory order and wording, do not change the
    interview flow, questions, or topic list). In skills/writer/profile-view.md: change "Step 1:
    Load profile" so it reads PROFILE.md (and the other three profile files) first, falling back
    to memory only if the files don't exist yet, instead of the current memory-first order; keep
    the rest of the display formatting unchanged. In skills/writer/profile-edit.md: change "Step
    1: Load current profile" to read the profile files first; change "Step 4: Update both memory
    and file" to reverse the order so the file updates first and the memory-tool entry updates
    second (rename the step "Update file, then memory cache" and swap the two bullet points'
    order). In skills/writer/profile-delete.md: change "Step 1: Load and inventory" to count
    entries from the profile files first (falling back to memory only if a file is missing);
    change "Step 4: Delete" so it deletes the four profile files first and then the equivalent
    Claude Code memory-tool entries second (currently listed in the opposite order). None of the
    four files' confirmation prompts, interview questions, or user-facing wording should change.
  </action>
  <verify>
    <automated>grep -Ec "file (first|second)" skills/writer/profile-create.md skills/writer/profile-view.md skills/writer/profile-edit.md skills/writer/profile-delete.md | awk -F: '{s+=$2} END{if (s==0) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four files' load/save/delete steps lists a file-based action before the
      equivalent memory-tool action, wherever the two appear together
    - profile-create.md's interview questions (the 10-topic list) are byte-for-byte unchanged
    - profile-delete.md still requires the same explicit confirmation phrase before deleting
      anything
    - profile-view.md's displayed section layout (Identity/Audience/Voice/Content
      Strategy/Publishing/Products/CTAs/Case Studies) is unchanged
  </acceptance_criteria>
  <done>
    All four profile commands read/write the profile files as the primary action, with memory as
    a secondary cache, and no user-facing behavior changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate next, status, and touch help/update</name>
  <files>
    skills/writer/next.md, skills/writer/status.md, skills/writer/help.md, skills/writer/update.md
  </files>
  <read_first>
    skills/writer/next.md (full), skills/writer/status.md (full), skills/writer/help.md (full),
    skills/writer/update.md (full), skills/state-schema.md (Phase State Machine section)
  </read_first>
  <action>
    In skills/writer/next.md: replace the entire "Phase detection logic" numbered list (currently
    seven steps keyed on memory-key presence) with the phase-field table from
    skills/state-schema.md's Phase State Machine section: no PROJECT-STATE.md file and no profile
    -> run /writer:profile-create; profile exists, no PROJECT-STATE.md file -> run
    /writer:discuss; phase: discuss -> run /writer:plan; phase: plan -> run /writer:execute;
    phase: execute -> run /writer:verify; phase: verify -> run /writer:ship; phase: complete ->
    "Workflow complete. Run /writer:discuss to start a new project." Keep the exact same
    announcement-message wording for each branch, only the detection condition changes. In
    skills/writer/status.md: replace "Step 1: Gather state" and the state table in "Step 2:
    Determine current phase" so both check PROJECT-STATE.md's `phase` field instead of individual
    memory-key presence, using the same phase-value mapping as next.md; keep the display format
    in "Step 3: Display" unchanged. In skills/writer/help.md and skills/writer/update.md: read
    both fully; if either contains any instruction that assumes memory is the only place project
    or profile state lives (beyond the session-only "updateNotificationShown" flag, which is a
    Claude-Code-only convenience feature and should be left as-is), rewrite that instruction to
    match the file-first model; if neither file makes such an assumption beyond the
    session-tracking flag, make no changes to them and state that explicitly in the plan's
    SUMMARY.
  </action>
  <verify>
    <automated>grep -c "phase: discuss" skills/writer/next.md skills/writer/status.md | awk -F: '{if ($2==0) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - next.md's phase-detection logic and status.md's phase table both use the literal field-value
      forms "phase: discuss", "phase: plan", "phase: execute", "phase: verify" as their conditions
    - next.md's and status.md's user-facing announcement/display text is otherwise unchanged
    - help.md and update.md's `updateNotificationShown` session-flag mechanism is left untouched
      (it is a Claude-Code-only UX convenience, not part of the portable project/profile schema,
      and is out of scope for this migration)
    - `node scripts/verify-writer-commands.js --check=migration` reports no remaining pre-migration
      phrasing in any of the four files touched by this task
  </acceptance_criteria>
  <done>
    next.md and status.md detect phase via the PROJECT-STATE.md `phase` field; help.md and
    update.md are confirmed clean or updated as needed, with no behavior change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Profile files on disk -> profile-delete.md's inventory/delete steps | Deletion now reads file contents to build the "this will delete N products/CTAs/case studies" confirmation summary shown to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-05 | Tampering | profile-delete.md file-based inventory count | low | accept | Miscounting due to a hand-edited profile file only affects the confirmation message shown to the file's own owner before their own explicit delete confirmation — no elevated-privilege or cross-user impact |
| T-02-06 | Information Disclosure | profile-view.md file-first read | low | accept | Same data the command already displayed from memory is now displayed from the file; no new data is exposed |

</threat_model>

<verification>
`node scripts/verify-writer-commands.js --check=migration` passes for all eight files across this
plan. Diff review confirms no interview questions, confirmation phrases, or display formats
changed.
</verification>

<success_criteria>
All four profile commands and next.md/status.md are file-first; help.md/update.md confirmed clean
or updated; ADAPTER-01's structural migration is complete across all thirteen writer/*.md files
plus shared-context.md.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-05-SUMMARY.md` when done
</output>
