---
phase: 01-multi-platform-portability-layer
plan: 08
type: execute
wave: 3
depends_on: ["01-01", "01-06"]
files_modified:
  - skills/adapters/claude-skill/SKILL.md
  - skills/adapters/claude-skill/references/
autonomous: true
requirements: [ADAPTER-02]

user_setup:
  - service: claude.ai / Claude API Skills
    why: "Uploading a custom Skill bundle requires the claude.ai or Claude API builder UI — no CLI/API exists for this that Claude Code can drive itself"
    dashboard_config:
      - task: "Upload the skills/adapters/claude-skill/ folder as a custom Skill"
        location: "claude.ai -> Settings -> Capabilities -> Skills (chat), or the Claude API Skills upload endpoint for API integrators"

must_haves:
  truths:
    - "A claude.ai or Claude API user can upload skills/adapters/claude-skill/ as a Skill bundle and have it trigger by description, the same way the existing top-level SKILL.md triggers in Claude Code — no slash command required"
    - "The bundle's SKILL.md instructs explicit fallback behavior when code-execution has no network access, instead of silently failing to fetch a blog URL"
  artifacts:
    - path: "skills/adapters/claude-skill/SKILL.md"
      provides: "Claude.ai / Claude API Skill packaging (ADAPTER-02)"
  key_links:
    - from: "skills/adapters/claude-skill/SKILL.md"
      to: "skills/adapters/claude-skill/references/"
      via: "the bundle's reference-file table points at the references/ folder generated by scripts/build-adapter-knowledge.js, not a hand-duplicated copy"
      pattern: "references/"
---

<objective>
Package the content-strategy engine as a description-triggered Claude.ai / Claude API Skill
bundle, reusing the same reference docs every other adapter reads, adapted for a chat/API
invocation model instead of Claude Code's slash commands.

Purpose: ADAPTER-02 requires a Claude.ai (chat + API) Skills adapter runnable in claude.ai's
skill/code-execution environment, matching Claude's actual skill-invocation model
(description-triggered, not command-triggered) per RESEARCH.md's official spec citation.
Output: `skills/adapters/claude-skill/SKILL.md` plus its generated `references/` folder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Skill bundle's SKILL.md</name>
  <files>skills/adapters/claude-skill/SKILL.md</files>
  <read_first>
    SKILL.md (top-level, repo root — its existing frontmatter and five-phase description are the
    starting point, already description-triggered and close to the correct shape per
    RESEARCH.md), skills/state-schema.md (Project State Schema, File-First Storage Rule),
    .planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md ("Code Examples" > SKILL.md
    frontmatter constraints, "Pitfall 1: Assuming network access", "Pitfall 2: Treating knowledge
    files as a live filesystem", "Code Examples" > container-reuse pattern)
  </read_first>
  <action>
    Create skills/adapters/claude-skill/SKILL.md with YAML frontmatter `name: content-writer`
    (reuse the existing top-level SKILL.md's name — it is already lowercase-hyphenated, under 64
    characters, and contains neither "anthropic" nor "claude") and a `description` field adapted
    from the top-level SKILL.md's description (under 1024 characters, listing the content types
    and trigger phrases, unchanged in substance). In the body, adapt the five-phase workflow
    section from the top-level SKILL.md for chat/API context: replace every "Save state to
    memory as [key]" instruction with "Update content-writer-output/profile/PROJECT-STATE.md's
    `phase` field and the relevant frontmatter fields per skills/state-schema.md" (using the exact
    field names from that schema). Add an explicit branch to the profile-creation URL-scanning
    step and the execute-phase blog-URL-fetch step: "If code-execution has network access in this
    session, fetch the URL directly; if not, ask the user to paste the article text or 2-3
    representative paragraphs instead" — this closes RESEARCH.md's Pitfall 1. Add a note under the
    Storage Pattern section stating that when running as an API integration, the caller should
    reuse the same `container.id` across turns within one project to keep PROJECT-STATE.md
    present in the execution filesystem without re-uploading it every message, but must always
    re-read the file at the start of each phase rather than assume in-memory continuity — this is
    the RESEARCH.md-recommended default (re-supply each phase), with container reuse framed as an
    optimization, not a requirement. Add a "Reference Files" table pointing at
    skills/adapters/claude-skill/references/ (the folder generated by
    scripts/build-adapter-knowledge.js, not restated inline) for every content-type-specific
    convention file, matching the load-only-what's-needed discipline already in the top-level
    SKILL.md. Do not restate the full five-phase interview questions differently from the
    top-level SKILL.md — this file adapts storage/invocation mechanics, not the content strategy
    itself.
  </action>
  <verify>
    <automated>test -f skills/adapters/claude-skill/SKILL.md && grep -c "^name: content-writer" skills/adapters/claude-skill/SKILL.md && grep -c "PROJECT-STATE.md" skills/adapters/claude-skill/SKILL.md && grep -c "network access" skills/adapters/claude-skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/claude-skill/SKILL.md's frontmatter `name` field is exactly `content-writer`
      (64 characters or fewer, lowercase/hyphen only, does not contain "claude" or "anthropic" as
      a substring)
    - Its `description` field is present and non-empty, under 1024 characters
    - It references content-writer-output/profile/PROJECT-STATE.md for state, not a memory-key
      scheme
    - It contains an explicit network-access fallback branch for URL-fetching steps
    - It references skills/adapters/claude-skill/references/ for content-type conventions rather
      than restating file contents inline
  </acceptance_criteria>
  <done>
    skills/adapters/claude-skill/SKILL.md exists, is spec-compliant, file-first for state, and
    handles the no-network-access case explicitly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate the bundle's reference folder</name>
  <files>skills/adapters/claude-skill/references/ (generated, not hand-written)</files>
  <read_first>scripts/build-adapter-knowledge.js (the --target=claude-skill code path)</read_first>
  <action>
    Run `node scripts/build-adapter-knowledge.js --target=claude-skill` to (re)populate
    skills/adapters/claude-skill/references/ from the canonical references/ directory plus
    skills/state-schema.md. Do not hand-edit any file inside skills/adapters/claude-skill/references/
    directly — if a reference file needs a change, that change belongs in the canonical
    references/ directory, followed by re-running this build command.
  </action>
  <verify>
    <automated>node scripts/build-adapter-knowledge.js --target=claude-skill && test "$(ls skills/adapters/claude-skill/references | wc -l | tr -d ' ')" = "13"</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/claude-skill/references/ contains exactly 13 files (12 canonical reference
      docs plus state-schema.md)
    - Every file's content is byte-for-byte identical to its source under references/ or
      skills/state-schema.md
  </acceptance_criteria>
  <done>skills/adapters/claude-skill/references/ is populated and in sync with the canonical source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Third-party Skill bundle trust | RESEARCH.md's official security guidance for Skills applies to this bundle too — a user's Claude Code/Claude.ai instance running alongside other installed Skills must be able to audit this bundle's files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-08-01 | Tampering / Elevation of Privilege | skills/adapters/claude-skill/ bundle contents | low | mitigate | The bundle contains only markdown reference docs and one SKILL.md — no scripts, no obfuscated code, no unexplained network calls, matching the official "audit all bundled files, avoid unexplained network calls" guidance this bundle asks other Skill authors to follow |
| T-01-08-02 | Information Disclosure | API container-ID reuse note | low | mitigate | The note explicitly instructs treating a reused container ID like any other bearer credential — not logged, not embedded in shareable PROJECT-STATE.md content |

</threat_model>

<verification>
skills/adapters/claude-skill/SKILL.md passes spec-compliance checks; references/ folder contains
13 files matching the canonical source exactly.
</verification>

<success_criteria>
A complete, spec-compliant Claude.ai / Claude API Skill bundle exists, built from the same
reference docs as every other adapter, with explicit network-access fallback handling.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-08-SUMMARY.md` when done
</output>
