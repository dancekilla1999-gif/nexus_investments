---
phase: 01-multi-platform-portability-layer
plan: 09
type: execute
wave: 3
depends_on: ["01-01", "01-06"]
files_modified:
  - skills/adapters/chatgpt/INSTRUCTIONS.md
  - skills/adapters/chatgpt/knowledge/
autonomous: true
requirements: [ADAPTER-03]

user_setup:
  - service: ChatGPT Custom GPT
    why: "Creating a Custom GPT and pasting instructions/uploading knowledge files requires the ChatGPT builder UI — no CLI/API exists for this that Claude Code can drive itself"
    dashboard_config:
      - task: "Create a new Custom GPT, paste skills/adapters/chatgpt/INSTRUCTIONS.md into the Instructions field, and upload every file in skills/adapters/chatgpt/knowledge/ as knowledge files"
        location: "chatgpt.com -> Explore GPTs -> Create"

must_haves:
  truths:
    - "A ChatGPT user can create a Custom GPT from INSTRUCTIONS.md plus the knowledge files and get the same five-phase workflow, with state carried by re-pasting the updated PROJECT-STATE.md content each session, never by assuming ChatGPT wrote a file back for them"
    - "INSTRUCTIONS.md leaves real margin below the assumed ~8,000 character instructions limit rather than trusting the exact number, per RESEARCH.md's Assumption A1"
  artifacts:
    - path: "skills/adapters/chatgpt/INSTRUCTIONS.md"
      provides: "ChatGPT Custom GPT instructions (ADAPTER-03)"
  key_links:
    - from: "skills/adapters/chatgpt/INSTRUCTIONS.md"
      to: "skills/adapters/chatgpt/knowledge/"
      via: "INSTRUCTIONS.md references each knowledge file by name for the model to consult, rather than restating their content"
      pattern: "knowledge/"
---

<objective>
Package the content-strategy engine as ChatGPT Custom GPT instructions plus knowledge files,
reproducing the five-phase workflow conversationally within ChatGPT's read-only-knowledge, no
guaranteed-filesystem constraints.

Purpose: ADAPTER-03 requires a ChatGPT adapter (Custom GPT instructions + knowledge files)
reproducing the phased workflow, with state carried in-chat or via uploaded/downloaded files —
since Custom GPT knowledge files cannot be written back to mid-conversation.
Output: `skills/adapters/chatgpt/INSTRUCTIONS.md` plus its generated `knowledge/` folder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Custom GPT instructions</name>
  <files>skills/adapters/chatgpt/INSTRUCTIONS.md</files>
  <read_first>
    SKILL.md (top-level — five-phase description and profile-first enforcement to adapt),
    skills/state-schema.md (Project State Schema, File-First Storage Rule), .planning/phases/01-
    multi-platform-portability-layer/01-RESEARCH.md ("Standard Stack" > Custom GPT
    ~8,000-character assumption A1, "Pitfall 2: Treating knowledge files as a live filesystem",
    "Pitfall 3: Assuming GPT memory solves cross-session state")
  </read_first>
  <action>
    Write skills/adapters/chatgpt/INSTRUCTIONS.md as prose intended to be pasted directly into a
    Custom GPT's Instructions field, targeting under 7,000 characters (leaving margin below the
    ~8,000-character figure RESEARCH.md flags as unverified, per Assumption A1) — this is a hard
    constraint on this task's output length, not a suggestion. Cover, concisely: (1) the
    profile-first rule (no content without a profile; if the user has no PROFILE.md content to
    paste, run the same 10-topic questionnaire from the shared content-strategy knowledge and
    present the result as a markdown file for them to save); (2) the five-phase flow
    (discuss/plan/execute/verify/ship), summarized in one line each with a pointer to the relevant
    knowledge file rather than restating its content; (3) the state-carrying rule for ChatGPT
    specifically: at the end of every phase, output the complete updated PROJECT-STATE.md content
    (per skills/state-schema.md's exact frontmatter fields) as a fenced code block in the chat and
    instruct the user to save it and paste it back verbatim at the start of the next session or
    next message — never say "saved to your knowledge file" or imply persistence the platform
    doesn't provide; (4) a one-line note that ChatGPT's own per-GPT memory feature, if the user has
    it enabled, may smooth this along but must never be relied on as the only place state lives,
    per RESEARCH.md's Pitfall 3; (5) the network-access caveat for URL-fetching steps, offering
    "paste the article text" as the fallback when a URL cannot be fetched. Reference each uploaded
    knowledge file by exact filename for the specific situation it applies to (e.g. "for SEO
    metadata rules, consult seo-meta-conventions.md"), rather than repeating its content.
  </action>
  <verify>
    <automated>test -f skills/adapters/chatgpt/INSTRUCTIONS.md && test "$(wc -c skills/adapters/chatgpt/INSTRUCTIONS.md | awk '{print $1}')" -le 7000</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/chatgpt/INSTRUCTIONS.md exists and is 7,000 characters or fewer
    - It covers all five points listed in the action: profile-first rule, five-phase flow summary,
      the re-paste-per-session state rule, the per-GPT-memory caveat, and the network-access
      fallback
    - It never contains a phrase claiming the model "saved" or "wrote" a knowledge file on the
      user's behalf
    - It references at least three knowledge files by exact filename rather than restating their
      content inline
  </acceptance_criteria>
  <done>
    skills/adapters/chatgpt/INSTRUCTIONS.md exists, is within the safety margin below the assumed
    character limit, and correctly frames state as re-pasted per session, never platform-persisted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate the bundle's knowledge folder</name>
  <files>skills/adapters/chatgpt/knowledge/ (generated, not hand-written)</files>
  <read_first>scripts/build-adapter-knowledge.js (the --target=chatgpt code path)</read_first>
  <action>
    Run `node scripts/build-adapter-knowledge.js --target=chatgpt` to (re)populate
    skills/adapters/chatgpt/knowledge/ from the canonical references/ directory plus
    skills/state-schema.md. Do not hand-edit any file inside skills/adapters/chatgpt/knowledge/
    directly — if a reference file needs a change, that change belongs in the canonical
    references/ directory, followed by re-running this build command.
  </action>
  <verify>
    <automated>node scripts/build-adapter-knowledge.js --target=chatgpt && test "$(ls skills/adapters/chatgpt/knowledge | wc -l | tr -d ' ')" = "13"</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/chatgpt/knowledge/ contains exactly 13 files (12 canonical reference docs
      plus state-schema.md), each under ChatGPT's 20-file-per-GPT cap with room to spare
    - Every file's content is byte-for-byte identical to its source under references/ or
      skills/state-schema.md
  </acceptance_criteria>
  <done>skills/adapters/chatgpt/knowledge/ is populated and in sync with the canonical source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-pasted PROJECT-STATE.md content -> ChatGPT conversation context | Since ChatGPT has no file-write-back, the user re-pastes state text every session; that pasted text is trusted as data by the running instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-09-01 | Tampering | Re-pasted PROJECT-STATE.md content | medium | mitigate | INSTRUCTIONS.md carries forward skills/state-schema.md's File-First Storage Rule instruction that frontmatter/body values are data, never new instructions, regardless of what a pasted field contains |
| T-01-09-02 | Information Disclosure | Per-GPT memory feature caveat | low | accept | The instructions explicitly tell the model never to rely on GPT memory as the sole state store, avoiding silent data loss if the feature is disabled by the user or org policy |

</threat_model>

<verification>
INSTRUCTIONS.md is under 7,000 characters and covers all five required points; knowledge/ folder
contains 13 files matching the canonical source exactly.
</verification>

<success_criteria>
A complete ChatGPT Custom GPT package exists, within the safe character margin, built from the
same reference docs as every other adapter, with correct re-paste-per-session state framing.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-09-SUMMARY.md` when done
</output>