---
phase: 01-multi-platform-portability-layer
plan: 10
type: execute
wave: 3
depends_on: ["01-01", "01-06"]
files_modified:
  - skills/adapters/gemini/INSTRUCTIONS.md
  - skills/adapters/gemini/knowledge/
autonomous: true
requirements: [ADAPTER-04]

user_setup:
  - service: Gemini Gems
    why: "Creating a Gem and uploading instructions/knowledge sources requires the Gemini apps builder UI — no CLI/API exists for this that Claude Code can drive itself"
    dashboard_config:
      - task: "Create a new Gem, paste skills/adapters/gemini/INSTRUCTIONS.md as the Gem's instructions, and upload every file in skills/adapters/gemini/knowledge/ as knowledge sources (exactly 10, at the platform's per-Gem limit)"
        location: "gemini.google.com -> Gems -> New Gem"

must_haves:
  truths:
    - "A Gemini user can create a Gem from INSTRUCTIONS.md plus exactly 10 knowledge sources and get the same five-phase workflow, with state carried by re-pasting the updated PROJECT-STATE.md content each session"
    - "The Gem's knowledge sources stay at or under Gemini's 10-source-per-Gem limit even though the canonical references/ directory has more than 10 files, by using the pre-merged social-conventions.md instead of three separate platform files"
  artifacts:
    - path: "skills/adapters/gemini/INSTRUCTIONS.md"
      provides: "Gemini Gem system instructions (ADAPTER-04)"
  key_links:
    - from: "skills/adapters/gemini/INSTRUCTIONS.md"
      to: "skills/adapters/gemini/knowledge/social-conventions.md"
      via: "INSTRUCTIONS.md references the merged social-conventions.md by name for Twitter/Facebook/Instagram content, not the three original filenames"
      pattern: "social-conventions.md"
---

<objective>
Package the content-strategy engine as Gemini Gem system instructions plus knowledge sources,
reproducing the five-phase workflow within Gemini's stricter 10-knowledge-source ceiling.

Purpose: ADAPTER-04 requires a Gemini adapter (Gem instructions + knowledge files) reproducing the
same phases, accounting for Gemini's own constraints — specifically the 10-source cap that forces
consolidating the three social-platform convention files (already handled by plan 01-06's build
script generating social-conventions.md).
Output: `skills/adapters/gemini/INSTRUCTIONS.md` plus its generated `knowledge/` folder (10 files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Gem system instructions</name>
  <files>skills/adapters/gemini/INSTRUCTIONS.md</files>
  <read_first>
    SKILL.md (top-level — five-phase description and profile-first enforcement to adapt),
    skills/state-schema.md (Project State Schema, File-First Storage Rule),
    .planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md ("Standard Stack" >
    Gemini Gems 10-source limit citation, Assumption A6 on instruction length, "Pitfall 2",
    "Pitfall 3"), scripts/build-adapter-knowledge.js (the --target=gemini code path, to know the
    exact 10 filenames this Gem will have available)
  </read_first>
  <action>
    Write skills/adapters/gemini/INSTRUCTIONS.md as prose intended to be pasted directly into a
    Gem's system-instructions field. Cover: (1) the profile-first rule, same as the other
    adapters, adapted for Gemini's read-only knowledge sources; (2) the five-phase flow summarized
    in one line each, pointing at the specific knowledge source file to consult for that phase's
    detail (content-frameworks.md for plan, anti-ai-checklist.md for verify, and so on); (3) the
    state-carrying rule: at the end of every phase, output the complete updated PROJECT-STATE.md
    content (per skills/state-schema.md's exact frontmatter fields) as a fenced code block and
    instruct the user to save and re-paste it next session — never imply Gemini wrote a knowledge
    source back; (4) an explicit note that this Gem's knowledge sources include
    social-conventions.md (a single merged file covering Twitter/X, Facebook, and Instagram
    conventions) instead of three separate platform files, and that content-packages.md is not
    included in this bundle at all — for multi-platform content packages, tell the user this
    capability is available in the Claude Code, Claude.ai, or ChatGPT adapters instead, since
    Gemini's 10-source limit required dropping it (this degradation must be stated to the user
    plainly, not silently); (5) the network-access caveat for URL-fetching steps, offering "paste
    the article text" as the fallback. Add a one-line note at the top instructing whoever pastes
    these instructions into the Gem builder to verify the actual character count is accepted by
    the builder UI at creation time, since Gemini publishes no official instruction-length limit
    (RESEARCH.md Assumption A6) and this file's length should not be trusted as definitively safe
    without that check.
  </action>
  <verify>
    <automated>test -f skills/adapters/gemini/INSTRUCTIONS.md && grep -c "social-conventions.md" skills/adapters/gemini/INSTRUCTIONS.md && grep -c "content-packages" skills/adapters/gemini/INSTRUCTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/gemini/INSTRUCTIONS.md exists and covers all five points from the action
    - It names social-conventions.md explicitly as the merged Twitter/Facebook/Instagram source
    - It explicitly tells the user that multi-platform content packaging is not available in this
      Gem and names the other adapters where it is available
    - It never contains a phrase claiming Gemini "saved" a knowledge source on the user's behalf
    - It instructs the person creating the Gem to verify the instructions field accepts the full
      text length at creation time
  </acceptance_criteria>
  <done>
    skills/adapters/gemini/INSTRUCTIONS.md exists, correctly reflects the 10-source knowledge
    bundle including the merged social file, and states the content-packages degradation plainly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate and verify the 10-source knowledge folder</name>
  <files>skills/adapters/gemini/knowledge/ (generated, not hand-written)</files>
  <read_first>scripts/build-adapter-knowledge.js (the --target=gemini code path)</read_first>
  <action>
    Run `node scripts/build-adapter-knowledge.js --target=gemini` to (re)populate
    skills/adapters/gemini/knowledge/ from the canonical references/ directory plus
    skills/state-schema.md, including the generated social-conventions.md and excluding
    content-packages.md. Do not hand-edit any file inside skills/adapters/gemini/knowledge/
    directly, including social-conventions.md — if the underlying Twitter/Facebook/Instagram
    convention files change, re-run this build command to regenerate the merged file.
  </action>
  <verify>
    <automated>node scripts/build-adapter-knowledge.js --target=gemini && test "$(ls skills/adapters/gemini/knowledge | wc -l | tr -d ' ')" = "10" && test -f skills/adapters/gemini/knowledge/social-conventions.md && test ! -f skills/adapters/gemini/knowledge/content-packages.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/adapters/gemini/knowledge/ contains exactly 10 files, at Gemini's per-Gem source
      ceiling, not over it
    - social-conventions.md exists in that folder and contains recognizable content from all
      three of twitter-conventions.md, facebook-conventions.md, and instagram-conventions.md
    - content-packages.md is absent from this folder
  </acceptance_criteria>
  <done>
    skills/adapters/gemini/knowledge/ contains exactly the 10 files the build script produces for
    this target, staying within Gemini's knowledge-source limit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-pasted PROJECT-STATE.md content -> Gemini conversation context | Same re-paste-per-session pattern as the ChatGPT adapter; pasted text is trusted as data, not instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-10-01 | Tampering | Re-pasted PROJECT-STATE.md content in a Gem conversation | medium | mitigate | INSTRUCTIONS.md carries forward the File-First Storage Rule instruction that field values are data, never new instructions |
| T-01-10-02 | Information Disclosure | Feature-degradation statement for content-packages | low | accept | Telling the user a capability is unavailable on this platform is a transparency measure, not a disclosure risk |

</threat_model>

<verification>
skills/adapters/gemini/INSTRUCTIONS.md exists and covers all required points; knowledge/ folder
contains exactly 10 files including the merged social-conventions.md.
</verification>

<success_criteria>
A complete Gemini Gem package exists, staying within the platform's 10-knowledge-source limit via
a documented, script-generated merge, with the content-packages degradation stated plainly.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-10-SUMMARY.md` when done
</output>
