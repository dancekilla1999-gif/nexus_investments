---
phase: 01-multi-platform-portability-layer
plan: 11
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05", "01-06", "01-07", "01-08", "01-09", "01-10"]
files_modified:
  - docs/setup-claude-code.md
  - docs/setup-claude-ai.md
  - docs/setup-chatgpt.md
  - docs/setup-gemini.md
  - docs/setup-headless.md
  - docs/feature-degradation-matrix.md
  - package.json
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "A user landing on any one of the five setup guides can go from zero to a working adapter on that platform using only the exact steps in that guide — no cross-referencing required"
    - "The feature-degradation matrix tells a user, before they pick a platform, exactly which capabilities (URL tone scanning, auto-update check, SEO/humanizer integration, file persistence) they will lose or have to work around on that platform"
  artifacts:
    - path: "docs/setup-claude-code.md"
      provides: "Per-platform setup guide (DOCS-01)"
    - path: "docs/feature-degradation-matrix.md"
      provides: "Feature-degradation matrix (DOCS-02)"
  key_links:
    - from: "docs/feature-degradation-matrix.md"
      to: "skills/adapters/gemini/INSTRUCTIONS.md"
      via: "the matrix's Gemini row states the content-packages exclusion documented in that adapter's instructions"
      pattern: "content-packages"
---

<objective>
Write the five per-platform setup guides and the feature-degradation matrix that let a user pick a
platform and get running on it, understanding exactly what they gain and lose compared to Claude
Code.

Purpose: DOCS-01 requires exact steps to load/run this system on each of the five surfaces.
DOCS-02 requires a matrix of which capabilities degrade or fall back on which platform, and how.
This plan runs after every adapter is built (Wave 3) and the Claude Code migration is complete
(Wave 2), so it documents the actual shipped artifacts, not a plan for them.
Output: docs/setup-*.md (5 files), docs/feature-degradation-matrix.md, package.json's `files`
array updated to publish the new docs/ directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the five per-platform setup guides</name>
  <files>
    docs/setup-claude-code.md, docs/setup-claude-ai.md, docs/setup-chatgpt.md,
    docs/setup-gemini.md, docs/setup-headless.md
  </files>
  <read_first>
    README.md (existing install instructions to reuse for the Claude Code guide),
    skills/adapters/claude-skill/SKILL.md, skills/adapters/chatgpt/INSTRUCTIONS.md,
    skills/adapters/gemini/INSTRUCTIONS.md, skills/adapters/mcp-server/README.md (the actual
    shipped adapters this plan documents setup for)
  </read_first>
  <action>
    Write docs/setup-claude-code.md: the existing `npx skills add sociilabs/claude-content-writer`
    install flow from README.md, plus a note that this migration (per ADAPTER-01) moved state
    storage to content-writer-output/profile/PROJECT-STATE.md and existing installs pick this up
    automatically on next `npm install`/postinstall via the one-time upgrade path. Write
    docs/setup-claude-ai.md: steps to upload the skills/adapters/claude-skill/ folder as a custom
    Skill via claude.ai's Settings -> Capabilities -> Skills (or the Claude API Skills upload
    endpoint for API integrators), and how to start a project once uploaded (trigger by
    description, e.g. "write me a LinkedIn post about X"). Write docs/setup-chatgpt.md: steps to
    create a Custom GPT, paste skills/adapters/chatgpt/INSTRUCTIONS.md into Instructions, and
    upload every file in skills/adapters/chatgpt/knowledge/ as knowledge files. Write
    docs/setup-gemini.md: steps to create a Gem, paste skills/adapters/gemini/INSTRUCTIONS.md, and
    upload every file in skills/adapters/gemini/knowledge/ (10 files) as knowledge sources. Write
    docs/setup-headless.md: steps to run `node skills/adapters/mcp-server/server/server.js`
    locally, connect an MCP-capable client (Claude Code's MCP config, Claude Desktop, or an
    automation platform's MCP client node) to it as a stdio subprocess, and a pointer to
    skills/adapters/mcp-server/README.md for the full tool contract. Every guide ends with a
    "Verify it worked" section pointing at the matching manual checklist under docs/checklists/
    for the three chat-platform guides (filenames to be created by a later plan in this phase —
    reference them by the exact path docs/checklists/claude-ai-manual-test.md,
    docs/checklists/chatgpt-manual-test.md, docs/checklists/gemini-manual-test.md even though they
    don't exist yet at the time this task runs) and at running the five /writer:* commands
    end-to-end for the Claude Code guide, and at skills/adapters/mcp-server/server/test-harness.js
    for the headless guide.
  </action>
  <verify>
    <automated>for f in docs/setup-claude-code.md docs/setup-claude-ai.md docs/setup-chatgpt.md docs/setup-gemini.md docs/setup-headless.md; do test -f "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All five docs/setup-*.md files exist
    - Each names the exact adapter artifact path it walks the user through setting up (e.g.
      docs/setup-chatgpt.md names skills/adapters/chatgpt/INSTRUCTIONS.md and
      skills/adapters/chatgpt/knowledge/ explicitly)
    - Each ends with a "Verify it worked" section pointing at a concrete verification artifact
    - docs/setup-claude-code.md explicitly states existing users are migrated automatically via
      the one-time upgrade path, with no manual action required
  </acceptance_criteria>
  <done>
    All five setup guides exist, each documenting the actual shipped adapter artifacts with exact
    steps and a verification pointer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the feature-degradation matrix and publish docs/</name>
  <files>docs/feature-degradation-matrix.md, package.json</files>
  <read_first>
    .planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md ("Architectural
    Responsibility Map", "Common Pitfalls" 1-4), skills/adapters/gemini/INSTRUCTIONS.md (its
    content-packages exclusion statement), package.json (current `files` array)
  </read_first>
  <action>
    Write docs/feature-degradation-matrix.md as a table with one row per capability — URL tone
    scanning (profile-create's blog/content-URL fetch), auto-update check (npm view version
    check), SEO integration (claude-seo), anti-AI integration (humanizer), multi-platform content
    packages, file persistence (writing/reading PROJECT-STATE.md automatically) — and one column
    per platform (Claude Code, Claude.ai/API, ChatGPT, Gemini, Headless/MCP), stating for each
    cell whether the capability works natively, degrades to a fallback (name the fallback exactly,
    e.g. "paste article text instead of URL fetch" or "manual SEO checklist"), or is unavailable
    entirely (name what the user should do instead, e.g. "use the Claude Code, Claude.ai, or
    ChatGPT adapter for multi-platform packages"). Base every cell on what the adapters built in
    this phase actually do, not aspirational behavior — cross-check the claude-skill/chatgpt/
    gemini INSTRUCTIONS.md and SKILL.md files' actual stated behavior for the URL-fetch fallback
    and the state-carrying rule cells. In package.json, add `"docs"` to the `files` array
    (alongside the existing "README.md", "CHANGELOG.md", "skills/", "references/", "scripts/"
    entries) so the new setup guides and degradation matrix are actually published to npm and
    reachable by an installed user, not just present in the git repo.
  </action>
  <verify>
    <automated>set -e; test -f docs/feature-degradation-matrix.md; node -e "const p=require('./package.json'); process.exit((p.files || []).includes('docs') ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/feature-degradation-matrix.md exists with one row per capability (URL tone scanning,
      auto-update check, SEO integration, anti-AI integration, multi-platform content packages,
      file persistence) and one column per platform (Claude Code, Claude.ai/API, ChatGPT, Gemini,
      Headless/MCP)
    - Every cell states native support, a named fallback, or a named unavailability with a
      redirect to another adapter — no cell is left blank or says only "degraded" without
      naming what happens instead
    - The Gemini column's multi-platform-content-packages row states the exclusion consistent
      with skills/adapters/gemini/INSTRUCTIONS.md
    - package.json's `files` array includes `"docs"` in addition to the five pre-existing entries
  </acceptance_criteria>
  <done>
    docs/feature-degradation-matrix.md exists and accurately reflects the real, shipped adapter
    behavior; package.json publishes the docs/ directory to npm.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> user's platform choice | Docs are advisory text only; no runtime trust boundary is introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-11-01 | Information Disclosure | package.json `files` array change | low | accept | Adding "docs" only affects which already-public repo files get published to the public npm registry; no private data is in docs/ |

</threat_model>

<verification>
All five setup guides and the degradation matrix exist with the required content; package.json
publishes docs/ to npm.
</verification>

<success_criteria>
DOCS-01 and DOCS-02 are both satisfied: every platform has an exact setup guide, and the
degradation matrix accurately documents what each platform gains or loses.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-11-SUMMARY.md` when done
</output>