---
phase: 01-multi-platform-portability-layer
plan: 12
type: execute
wave: 4
depends_on: ["01-08", "01-09", "01-10"]
files_modified:
  - docs/checklists/claude-ai-manual-test.md
  - docs/checklists/chatgpt-manual-test.md
  - docs/checklists/gemini-manual-test.md
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "A human who has never seen this repo can follow one checklist per chat platform, step by step, and know with certainty at the end whether that platform's adapter works end-to-end"
    - "Each checklist exercises the full Discuss -> Plan -> Execute -> Verify -> Ship cycle at least once, not just a single phase in isolation"
  artifacts:
    - path: "docs/checklists/claude-ai-manual-test.md"
      provides: "Claude.ai manual test checklist (VERIFY-01)"
    - path: "docs/checklists/chatgpt-manual-test.md"
      provides: "ChatGPT manual test checklist (VERIFY-01)"
    - path: "docs/checklists/gemini-manual-test.md"
      provides: "Gemini manual test checklist (VERIFY-01)"
  key_links:
    - from: "docs/checklists/chatgpt-manual-test.md"
      to: "skills/adapters/chatgpt/INSTRUCTIONS.md"
      via: "every checklist step references behavior actually documented in that adapter's INSTRUCTIONS.md, not aspirational behavior"
      pattern: "PROJECT-STATE.md"
---

<objective>
Write the three manual test checklists that let a human confirm each chat-platform adapter works
end-to-end, since none of claude.ai's, ChatGPT's, or Gemini's builder UIs can be scripted by the
building agent (locked project decision).

Purpose: VERIFY-01 requires a per-platform manual test checklist for exactly these three platforms
— concrete steps a human runs once, post-setup, confirming the full five-phase cycle works. This
plan runs after the three adapters exist (Wave 3), so each checklist references real, shipped
adapter content.
Output: docs/checklists/claude-ai-manual-test.md, chatgpt-manual-test.md, gemini-manual-test.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-multi-platform-portability-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Claude.ai manual test checklist</name>
  <files>docs/checklists/claude-ai-manual-test.md</files>
  <read_first>
    skills/adapters/claude-skill/SKILL.md (the actual shipped adapter this checklist tests),
    docs/setup-claude-ai.md (the setup steps this checklist assumes are already done)
  </read_first>
  <action>
    Write docs/checklists/claude-ai-manual-test.md as a numbered checklist, each item a single
    concrete action plus its expected PASS condition: (1) confirm the Skill bundle uploaded
    without an error in claude.ai's builder UI; (2) start a fresh conversation and say something
    that should trigger the Skill by description (not a slash command) — confirm it activates
    without the user having to name it explicitly; (3) if no profile exists yet, confirm the
    Skill runs the 10-topic questionnaire and, at the end, outputs PROFILE.md/PRODUCTS.md/
    CTAS.md/CASE-STUDIES.md content for the user to save; (4) run the discuss phase for a real
    topic and confirm it asks about topic/angle/platform/audience/awareness stage/goal/framework
    conversationally, not as a rigid form; (5) confirm it advances to plan and produces a
    section-by-section outline; (6) confirm it advances to execute and produces a full draft
    following the outline; (7) confirm it advances to verify and either runs a real SEO/anti-AI
    check or explicitly states it's using the manual checklist fallback; (8) confirm it advances
    to ship and outputs a final piece with frontmatter and publishing notes; (9) if a step in the
    flow needed to fetch a URL, confirm it either fetched successfully or explicitly asked for
    pasted text instead of silently failing (RESEARCH.md Pitfall 1); (10) confirm the final chat
    output includes the full updated project-state content in a form the user could paste
    elsewhere. End with a single PASS/FAIL summary line the tester fills in and a note to attach
    the failing step's transcript if anything failed.
  </action>
  <verify>
    <automated>test -f docs/checklists/claude-ai-manual-test.md; grep -c "PASS" docs/checklists/claude-ai-manual-test.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/checklists/claude-ai-manual-test.md contains at least 10 numbered checklist items
    - Every item names a concrete action and an explicit PASS condition, not just an action
    - The checklist covers all five phases (discuss/plan/execute/verify/ship) at least once each
    - The checklist includes the network-access fallback check and the profile-first check
  </acceptance_criteria>
  <done>
    docs/checklists/claude-ai-manual-test.md exists, is runnable by a first-time tester, and
    exercises the full five-phase cycle plus the profile-first and network-fallback behaviors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the ChatGPT and Gemini manual test checklists</name>
  <files>docs/checklists/chatgpt-manual-test.md, docs/checklists/gemini-manual-test.md</files>
  <read_first>
    skills/adapters/chatgpt/INSTRUCTIONS.md, skills/adapters/gemini/INSTRUCTIONS.md,
    docs/setup-chatgpt.md, docs/setup-gemini.md
  </read_first>
  <action>
    Write docs/checklists/chatgpt-manual-test.md following the same 10-item structure as the
    Claude.ai checklist (profile-first, all five phases, network-access fallback, final output
    format), with two ChatGPT-specific items replacing the Claude.ai-specific ones: confirm the
    Custom GPT was created successfully with the full INSTRUCTIONS.md text accepted by the builder
    UI without truncation (tester manually verifies the pasted length against what they see in the
    UI, since RESEARCH.md's ~8,000-character limit is unverified — Assumption A1); and confirm
    that at the end of each phase, the GPT explicitly outputs the full PROJECT-STATE.md content as
    a fenced code block and instructs the user to save/re-paste it, never claiming to have saved
    it as a knowledge file itself. Write docs/checklists/gemini-manual-test.md with the same
    10-item structure plus two Gemini-specific items: confirm all 10 knowledge sources uploaded
    successfully including social-conventions.md, and confirm the Gem, if asked to produce a
    multi-platform content package, correctly states that capability isn't available on this
    adapter and points the user at another adapter instead (per the degradation documented in
    skills/adapters/gemini/INSTRUCTIONS.md), rather than attempting it anyway or silently failing.
    Both files end with the same PASS/FAIL summary line and failing-transcript instruction as the
    Claude.ai checklist.
  </action>
  <verify>
    <automated>test -f docs/checklists/chatgpt-manual-test.md; test -f docs/checklists/gemini-manual-test.md; grep -c "PASS" docs/checklists/chatgpt-manual-test.md; grep -c "PASS" docs/checklists/gemini-manual-test.md</automated>
  </verify>
  <acceptance_criteria>
    - Both files contain at least 10 numbered checklist items each and cover all five phases
    - chatgpt-manual-test.md includes an explicit instructions-length verification step and a
      re-paste-not-saved verification step
    - gemini-manual-test.md includes an explicit 10-knowledge-source verification step and a
      content-packages-degradation verification step
    - Both files end with the same PASS/FAIL summary format as the Claude.ai checklist
  </acceptance_criteria>
  <done>
    docs/checklists/chatgpt-manual-test.md and gemini-manual-test.md exist, each tailored to its
    platform's specific constraints while covering the full five-phase cycle.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Documentation content -> human tester's judgment | Checklists are advisory instructions for a human; no runtime trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-12-01 | Repudiation | Missing failure evidence when a checklist step fails | low | mitigate | Every checklist ends with an explicit instruction to attach the failing step's transcript, so a reported failure is traceable |

</threat_model>

<verification>
All three checklist files exist with at least 10 items each, covering all five phases plus
platform-specific verification points.
</verification>

<success_criteria>
VERIFY-01 is satisfied: a human tester has a concrete, runnable checklist for each of the three
chat-platform adapters, tailored to that platform's real, shipped behavior.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-12-SUMMARY.md` when done
</output>
