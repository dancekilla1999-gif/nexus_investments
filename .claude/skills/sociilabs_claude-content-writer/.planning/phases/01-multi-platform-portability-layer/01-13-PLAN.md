---
phase: 01-multi-platform-portability-layer
plan: 13
type: execute
wave: 5
depends_on: ["01-03", "01-04", "01-05", "01-06", "01-07", "01-08", "01-09", "01-10", "01-11", "01-12"]
files_modified: []
autonomous: false
requirements: [ADAPTER-01, VERIFY-02]

must_haves:
  truths:
    - "A human running all five /writer:* phase commands in Claude Code end-to-end, after the full migration, sees identical behavior to before this phase started"
    - "Running the full structural lint with no flags exits 0, meaning the schema, the reference-doc neutralization, and every writer command's migration are all simultaneously correct at the same point in time"
    - "The MCP reference server's own test harness still passes after every other plan in this phase has landed, confirming no later plan silently broke it"
  artifacts: []
  key_links:
    - from: "scripts/verify-writer-commands.js"
      to: "skills/shared-context.md, skills/writer/*.md, references/*.md"
      via: "the no-flag full run checks all three areas simultaneously, only possible once every prior plan in this phase has landed"
      pattern: "exit 0"
---

<objective>
Close out the phase with the one gate that can only run after everything else has landed: a full
human regression pass over Claude Code's five commands, and a full automated structural-lint run
across the entire migrated repo.

Purpose: ADAPTER-01's zero-regression requirement can only be confirmed by a human actually
running the commands post-migration — there is no test harness for slash-command behavior in this
repo (per RESEARCH.md's Validation Architecture). VERIFY-02's automated check is only meaningful
as a full, no-flag run once every migration and neutralization plan in this phase has landed;
running it earlier only proves partial progress.
Output: none (verification-only plan; no new files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-multi-platform-portability-layer/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human regression pass on Claude Code's five commands</name>
  <files></files>
  <action>
    No file changes in this task — it is a human verification checkpoint. See &lt;how-to-verify&gt;
    below for the exact steps the human runs.
  </action>
  <what-built>
    The full ADAPTER-01 migration: skills/shared-context.md and all thirteen skills/writer/*.md
    commands now read and write content-writer-output/profile/PROJECT-STATE.md and the four
    profile files as the source of truth, with Claude Code's memory tool as an optional cache.
  </what-built>
  <how-to-verify>
    In a Claude Code session with this package installed (or the skills/ directory loaded
    directly), run the five phase commands end-to-end for one real content request:
    1. `/writer:profile-create` (if no profile exists yet) — confirm the same 10-topic interview
       as before this phase.
    2. `/writer:discuss "a topic of your choosing"` — confirm the same conversational questions
       about topic/angle/platform/audience/awareness stage/goal/framework.
    3. `/writer:plan` — confirm it produces a section-by-section outline and SEO strategy.
    4. `/writer:execute` — confirm it produces a full draft following the outline.
    5. `/writer:verify` — confirm the SEO/anti-AI checks (real or manual-fallback) run and are
       presented.
    6. `/writer:ship` — confirm the file is written under content-writer-output/[type]/ with
       frontmatter and publishing notes, and check that
       content-writer-output/profile/PROJECT-STATE.md now shows `phase: complete`.
    7. Run `/writer:status` and `/writer:next` once each — confirm both correctly report the
       current state at whatever point you check them.
    Compare the experience against your memory of this workflow before the migration (or against
    CHANGELOG.md's v2.2.0 description of expected behavior) — the questions asked, the outputs
    produced, and the file locations should all be identical. The only difference should be
    which file the state lives in.
  </how-to-verify>
  <resume-signal>Type "approved" if all six commands behaved identically to before, or describe exactly which step differed</resume-signal>
  <verify>
    <human-check>Human confirms all six commands (profile-create/discuss/plan/execute/verify/ship, plus status and next) behaved identically to pre-migration behavior</human-check>
  </verify>
  <done>Human has typed "approved", confirming zero observable regression in Claude Code's five-phase workflow post-migration.</done>
</task>

<task type="auto">
  <name>Task 2: Full automated lint and MCP harness re-run</name>
  <files></files>
  <read_first>
    scripts/verify-writer-commands.js, skills/adapters/mcp-server/server/test-harness.js
  </read_first>
  <action>
    Run `node scripts/verify-writer-commands.js` with no flags — this runs all three checks
    (schema, neutralize, migration) across the entire repo in its final, fully-migrated state. If
    it reports any failure, identify which prior plan's file is responsible from the reported
    file:line, and fix that specific file directly in this task (do not weaken the check itself
    to make it pass). Then run `node skills/adapters/mcp-server/server/test-harness.js` again to
    confirm no later plan in this phase altered content-writer-output/'s expected structure in a
    way that broke the reference MCP server's five behavior assertions. Both commands must exit 0
    before this task is done.
  </action>
  <verify>
    <automated>set -e; node scripts/verify-writer-commands.js; node skills/adapters/mcp-server/server/test-harness.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-writer-commands.js` (no flags) exits 0
    - `node skills/adapters/mcp-server/server/test-harness.js` exits 0
    - If either command initially failed, the fix is a correction to the specific file the check
      flagged, not a change to the check's own pass criteria
  </acceptance_criteria>
  <done>Both automated gates pass in the same run, confirming the full migration is internally consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full-repo gate -> phase completion decision | This plan's pass/fail result is the basis for declaring ADAPTER-01 and VERIFY-02 complete for the whole phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-13-01 | Repudiation | Weakening scripts/verify-writer-commands.js to force a pass | high | mitigate | Task 2's action explicitly forbids changing the check's pass criteria to resolve a failure — only the flagged file may be corrected |

</threat_model>

<verification>
Human confirms zero regression in Claude Code's five-phase workflow. Both automated commands
(the full structural lint and the MCP test harness) exit 0 in the same session.
</verification>

<success_criteria>
The entire Phase 1 migration is confirmed regression-free in Claude Code and internally
consistent end-to-end, with both automated gates green.
</success_criteria>

<output>
Create `.planning/phases/01-multi-platform-portability-layer/01-13-SUMMARY.md` when done
</output>
